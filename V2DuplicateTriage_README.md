# PR #12 重複候補の安全な自動仕分け

## 目的

`重複候補` シートに出た候補を、安全側に自動分類します。

- 施設名と住所が正規化後も両方一致する場合だけ `重複濃厚`
- それ以外は `要人確認`
- 元データは削除・変更しない
- `重複候補` シートの既存 `状態` 列も変更しない

## 追加列

`重複候補` シートの右端に次の3列を追加します。

- 推奨判定
- 自動判定理由
- 信頼度

## 安全ルール

### 重複濃厚

次をすべて満たす場合だけです。

1. 施設名が正規化後に完全一致
2. 住所が正規化後に完全一致

Place ID が同一なら信頼度99、Place ID がなくても施設名・住所が完全一致なら信頼度97とします。

### 要人確認

次は自動で重複確定しません。

- Place IDは同じだが住所が違う
- 同じ建物で階・部屋番号が違う
- 住所は同じだが施設名が違う
- 施設名は同じだが住所が違う
- 施設名または住所が不足
- 既存判定が `Place ID一致` でも、住所・施設名が完全一致しない

## 自動削除について

このPRには自動削除機能はありません。

`重複濃厚` はあくまで人が確認するための推奨判定です。

## 手動検証

1. Apps Scriptへ `V2DuplicateTriage.gs` を追加します。
2. `Code.gs` をPR #12版へ入れ替えます。
3. 関数 `runHotelDbV2DuplicateTriageTests` を実行します。
4. 次の結果を確認します。

```text
重複候補安全仕分けテスト 成功

成功件数: 16件
失敗件数: 0件
自動削除: なし
状態列の自動変更: なし
```

5. 次に人工的な重複テスト用シートを作り、⑦ `重複候補を更新` を実行します。
6. ⑩ `重複候補を自動仕分け` を実行します。
7. 完全一致のみ `重複濃厚`、別部屋・別階・施設名差などは `要人確認` になることを確認します。
8. 元データが削除・変更されていないことを確認します。

## 重要

PR #12の検証中も、⑧ `承認済み修正候補を反映` は実行しません。
