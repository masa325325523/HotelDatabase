# PR #23 製品全体ヘルスチェック

## 目的

宿泊施設DB Ver2.0の製品化フェーズとして、設定・実行入口・運用シート・安全設定・監査保護を1つの画面で確認できる読み取り専用ヘルスチェックを追加します。

## 利用者向け入口

メニュー最上部に次を追加します。

- `🩺 製品全体ヘルスチェック`

設定画面・バックアップ復元の近く、①〜⑲より前に配置します。

## 安全原則

- Google Places APIを自動では呼ばない
- APIキー本体を結果・HTML・ログ・シートへ返さない
- 元DBを変更しない
- 修正候補・要確認・重複候補などの運用シートを変更しない
- Script Propertiesを変更しない
- 診断前後で主要運用シートの値・数式とAPIキー指紋を比較する
- 診断中に状態変化を検出した場合は重大扱いにする

## 診断内容

### セットアップ

- APIキー設定済み / 未設定
- 現在のシートが元データとして利用可能か
- 管理・出力シートを元データとして誤認していないか

### 安全設定

- `GOOGLE_PLACES_API_KEY`
- `AUTO_ACCEPT_SCORE = 75`
- `MIN_MATCH_SCORE = 55`
- `BATCH_SIZE = 50`
- `TEST_ROWS = 3`
- `LANGUAGE_CODE = ja`
- `REGION_CODE = JP`
- `TIMEZONE = Asia/Tokyo`

### 機能入口

- 初期セットアップ
- 製品全体ヘルスチェック
- バックアップ・復元
- ①〜⑲の全メニュー実行関数

計22入口を確認します。

### 運用シート

コア5シートは存在と見出し互換を確認します。

- 修正候補
- 要確認
- 修正履歴
- 重複候補
- 実行サマリー

次のオンデマンドシートは存在状況を情報表示します。

- 新規追加候補
- 新規施設分類候補
- 閉業除外履歴
- 重複整理履歴
- 統合ダッシュボード

### 安全回帰スイート

既存の安全テスト入口がApps Scriptプロジェクトに存在することを確認します。

- PR #13 修正反映
- PR #17 新規追加
- PR #18 分類反映
- PR #19 閉業除外
- PR #20 重複整理
- PR #21 ダッシュボード
- PR #22 初期セットアップ
- PR #24 バックアップ・復元

ヘルスチェック中はこれらを自動実行しません。既存テストのUI表示や一時シート・Driveコピー生成を発生させず、製品利用者のワンクリック診断を読み取り専用に保つためです。

## 全体状態

- `正常`: warning / critical なし
- `要確認`: warningあり、criticalなし
- `重大`: criticalが1件以上

`info` は全体状態を悪化させません。

## UI

`V2HealthCheckDialog.html` で次を表示します。

- 全体状態
- 正常 / 要確認 / 重大 / 情報の件数
- 6セクションの詳細
- 次に確認すること
- 診断日時 / 所要時間
- 再診断ボタン

## 自己診断

`runHotelDbV2HealthCheckTests()`

PR #24統合後は現在の定義で101項目を確認します。

- 設定安全値
- 22実行入口
- 8安全回帰入口
- 集計・優先度・推奨ロジック
- SHA-256指紋
- 実環境の読み取り専用レポート
- APIキー本体非露出
- 診断前後の運用シート / APIキー不変

外部APIは呼びません。

## コピー版UIテスト

対象: `宿泊施設DB_PR13_⑧反映テスト`

1. `runHotelDbV2HealthCheckTests()`
2. `setupHotelDbV2HealthCheckUiTest()`
3. `testHotelDbV2HealthCheckUiTest()`
4. スプレッドシートを再読み込み
5. `宿泊施設DB → 🩺 製品全体ヘルスチェック`
6. 画面を目視確認
7. `cleanupHotelDbV2HealthCheckUiTest()`

setupはUser PropertiesへAPIキー「指紋」と運用シートの指紋だけを一時保存します。APIキー本体は保存しません。cleanupでテスト用User Propertiesを削除します。

## Out of Scope

- Google Places APIの自動接続確認
- 本番バッチの自動実行
- 候補の自動承認
- 修正・追加・除外・重複整理の自動実行
- バックアップ・復元の自動実行
- 既存個別自己診断の置換
- 運用シートの自動修正

## マージ前条件

- ヘルスチェック自己診断: 失敗0
- コピー版UIテスト成功
- 22実行入口が正常
- 8安全回帰入口が正常
- APIキー本体非露出
- Google Places API呼出なし
- 運用シート内容・数式不変
- Script Properties APIキー不変
- ヘルスチェック画面の目視確認
- cleanup成功

明示的な「マージして！」があるまでマージしません。
