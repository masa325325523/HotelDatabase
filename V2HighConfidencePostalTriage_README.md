# PR #9 高信頼郵便番号修正候補判定

## 目的

既存の「修正候補」で `要人確認` になっているケースのうち、郵便番号だけが実質的な修正対象である可能性が非常に高いものを、安全側の条件で `承認候補` へ再分類します。

元シートの郵便番号・市区町村・住所・施設名は変更しません。B列 `状態` も変更しません。

## 対象条件

次の条件をすべて満たす場合だけ対象です。

- 現在の推奨判定が `要人確認`
- Place IDあり
- Google営業状態が `営業中`
- 一致スコア83点以上
- 差分がちょうど `郵便番号・市区町村・住所`
- 元郵便番号とGoogle郵便番号がどちらも有効な7桁で、互いに異なる
- 施設名が正規化後に完全一致
- 元市区町村が `都道府県 + 郡`
- 元住所の先頭町村とGoogle市区町村が一致
- `郡 + 町村 + 番地` を組み直した完全住所が一致

条件を1つでも満たさない場合は `要人確認` のまま残します。

## 鳥取で想定している高信頼例

- 谷口旅館
- アルパインヒュッテ
- モリス荘
- 大江戸温泉物語 Premium 斉木別館
- ホテル東伯イン
- 河上旅館
- 北条オートキャンプ場

一方、ヒュッテ白樺のようにGoogle住所に追加所在地表記が付いて完全住所が一致しないものは自動承認しません。

## テスト

Apps Scriptへ `V2HighConfidencePostalTriage.gs` を追加し、次の関数を実行します。

`runHotelDbV2HighConfidencePostalTriageTests`

期待結果:

- 成功件数: 14件
- 失敗件数: 0件
- 状態列の自動変更: なし

## 実データ検証

テスト成功後に次を実行します。

`runHotelDbV2HighConfidencePostalTriage`

実行後は完了画面の `承認候補へ変更` 件数を確認し、変更された各行の施設名・郵便番号・住所を目視確認します。

## 安全性

- B列 `状態` は変更しない
- 元データは変更しない
- `⑧ 承認済み修正候補を反映` はこの検証段階では実行しない
- 施設名違い、番地違い、追加住所表記、低スコア、閉業、Place IDなしは自動承認しない
