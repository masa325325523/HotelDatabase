# 宿泊施設DB Ver2.0 製品リリースガイド

この文書は、開発用のGoogle Apps Scriptプロジェクトから、実運用・配布用の宿泊施設DB Ver2.0を切り出すときの安全基準をまとめたものです。

## PR #25 の目的

PR #25では自動デプロイやclasp導入は行いません。現在のリポジトリには `appsscript.json` や配布先Script IDを管理する仕組みがなく、そこまで同時に導入すると変更範囲が大きくなるためです。

代わりに、次を追加します。

- `🚀 リリース準備チェック`
- 製品リリースを止める条件の明示
- 開発用ファイルと製品用ファイルの分離ルール
- リリース前の手順書

## リリースを止める条件

次のどれか1つでも該当する場合は製品リリースしません。

1. APIキーが未設定
2. 元データシートを正しく認識できない
3. コア運用シートが未作成または見出し非互換
4. 製品全体ヘルスチェックに重大項目がある
5. 検証済みバックアップが1件もない
6. 復元方式が「本番ファイルを直接上書きしない」安全契約を満たさない
7. UIテスト用User Propertiesが残っている
8. リリース準備チェック中に元データ・APIキー状態が変化した
9. 実行対象がPRテスト・コピー・バックアップ・復元候補・作業中・開発ファイルである

## 製品用Apps Scriptに含めるもの

基本方針:

- 実運用の `.gs` は含める
- 実運用ダイアログの `.html` は含める
- APIキーはコードへ埋め込まず Script Properties の `GOOGLE_PLACES_API_KEY` にだけ保存する
- READMEはApps Scriptへ貼り付けない

## 開発用として製品配布から外すもの

次のパターンは開発・回帰確認用です。

- `*Tests.gs`
- `*UiTest.gs`
- `*_README.md`
- PR専用テスト補助ファイル

社内の「開発マスター」スプレッドシートには残して構いません。製品配布用コピーでは利用者に不要なテスト入口を減らすことを推奨します。

テストファイルを外した製品版では、`🩺 製品全体ヘルスチェック` の「安全回帰」欄に警告が出ることがあります。これは自己診断スイートが配布物に含まれないことを示す警告であり、重大項目ではありません。

## 推奨リリース手順

1. 最新 `main` を開発マスターへ反映
2. 元データシートを開く
3. `⚙ 初期セットアップ・設定` で「準備完了」を確認
4. 明示的に接続テストを1回実行
5. `🩺 製品全体ヘルスチェック` を実行し、重大項目を0件にする
6. `💾 バックアップ・復元` で検証済みバックアップを1件以上作る
7. `🚀 リリース準備チェック` を実行
8. 「リリース可能」を確認
9. 製品配布用コピーを作る
10. 製品配布用コピーから開発専用テストファイルを除外
11. APIキーが製品配布先のScript Propertiesへ正しく設定されているか確認
12. 製品版自身で `💾 バックアップ・復元` から検証済みバックアップを1件作成
13. 製品版で再度 `⚙` → `🩺` → `🚀` の順に確認

## バックアップの扱い

Google SheetsのコピーにはバインドされたApps Scriptも含まれるため、バックアップと復元候補は機密ファイルとして扱います。外部共有しません。

製品リリース準備チェックはバックアップを自動作成しません。存在確認だけを行います。

## APIキー

APIキー本体は以下へ出しません。

- シート
- 履歴
- リリース準備チェック結果
- ヘルスチェック結果
- バックアップ管理メタデータ
- GitHub

保存先は Script Properties の `GOOGLE_PLACES_API_KEY` だけです。

## 今後の自動パッケージ化

自動で「製品用ファイルだけ」を生成する場合は、別PRで `appsscript.json` / clasp / 配布先管理を設計します。PR #25では、既存の安全運用を壊さないため自動デプロイ基盤は導入しません。
