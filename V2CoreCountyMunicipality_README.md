# PR #8 本体 郡・町村表記差の同一判定

## 目的
PR #5で鳥取の実データ64件を使って安全性を確認した「郡と町村の列分け差」を、本体の `hotelDbV2CompareFacility_` に組み込みます。

例:

- 元データ: 市区町村=`鳥取県岩美郡` / 住所=`岩美町岩井536`
- Google: 市区町村=`鳥取県岩美町` / 住所=`岩美郡岩美町岩井536`

結合すると同一所在地であるため、安全条件をすべて満たす場合は最初から修正候補を作りません。

## 再利用する安全条件
PR #5の `hotelDbV2CountyMunicipalityDecision_` をそのまま呼び出します。

- Place IDあり
- Google営業状態=`営業中`
- 一致スコア90以上
- 差分が `市区町村` + `住所` の2項目だけ
- 郵便番号が正規化後に一致
- 施設名が正規化後に一致
- 元市区町村が `都道府県 + 郡`
- 元住所先頭の町村とGoogle市区町村が一致
- 郡・町村を結合した住所が同一
- `大字` の有無だけは許容

## 安全性
- 元シートの郵便番号・市区町村・住所・施設名を変更しない
- B列 `状態` を変更しない
- 番地違いは同一扱いしない
- 町村違いは同一扱いしない
- 郵便番号違いは同一扱いしない
- 施設名違いは同一扱いしない
- 一致スコア90未満は同一扱いしない
- 営業中以外は同一扱いしない
- Place IDなしは同一扱いしない
- PR #5の判定関数が存在しない環境では従来の差分判定へ安全にフォールバックする

## コード範囲
既存本番コードは `V2Sheets.gs` の差分判定へ25行を追加するだけです。既存行の削除はありません。

## 回帰テスト
Apps Scriptで `runHotelDbV2CoreCountyMunicipalityTests` を実行します。

期待結果:

- 成功件数: 14件
- 失敗件数: 0件
- 状態列の自動変更: なし

テストには岩美郡・八頭郡・東伯郡・日野郡の実データ型、`大字`省略、PR #7の全角郵便番号併用、番地違い・町違い・郵便番号違い・施設名違い・低スコア・閉業・Place IDなし等の否定ケースを含みます。

## 手動検証
1. Apps Scriptの `V2Sheets.gs` をPR #8版へ置き換える。
2. `V2CoreCountyMunicipalityTests.gs` を追加する。
3. `runHotelDbV2CoreCountyMunicipalityTests` を実行し14/14成功を確認する。
4. 鳥取の既知1件（例: 岩美郡/岩美町）を小さなテストシートで再照合する。
5. `修正候補: 0`、`要確認: 0`、エラー0となることを確認する。
6. 元の市区町村・住所が書き換わっていないことを確認する。
7. `⑧ 承認済み修正候補を反映` は実行しない。
