# PR #21 統合ダッシュボード

## 目的

①〜⑱まで増えた宿泊施設DB Ver2.0の運用状況を、1枚の `統合ダッシュボード` で把握できるようにします。

このPRでは新しい判定・自動修正・自動削除は追加しません。既存の候補・履歴・実行サマリーを読み取り、現在の状態と次の操作を見える化します。

## メニュー

`⑲ 統合ダッシュボードを更新`

実行すると `統合ダッシュボード` を新規作成または再生成します。

## 表示内容

### 最新バッチ実行

`実行サマリー` の最新行から次を表示します。

- 最新実行日時
- 元シート
- 開始行 / 終了行
- 処理件数
- 営業中
- 修正候補
- 要確認
- エラー
- 未検出
- 閉業
- 一時休業
- 開業予定
- スキップ
- 次回開始行
- 整合確認

### 候補ワークフロー状況

次の5シートを共通形式で集計します。

1. `修正候補`
2. `要確認`
3. `重複候補`
4. `新規追加候補`
5. `新規施設分類候補`

各シートについて以下を表示します。

- 総件数
- 未確認
- 承認
- 要再確認
- エラー
- 完了
- その他
- 次の操作

状態の分類は次のとおりです。

- 空欄 / `未確認` → 未確認
- `承認` → 承認
- `要再確認` → 要再確認
- `*エラー*` → エラー
- `反映済み` / `追加済み` / `除外済み` / `整理済み` → 完了
- それ以外 → その他

### 次にやること

最大5件を優先順で表示します。

1. エラー
2. 要再確認
3. 承認済み処理待ち
4. 未確認
5. バックログがない場合は④の続行、⑤再確認、⑬新規探索を案内

この表示は案内のみです。ダッシュボードから候補状態を変更したり、承認を自動実行したりはしません。

### 監査・完了実績

- `修正履歴` 件数
- `閉業除外履歴` の `除外済み` 件数
- `重複整理履歴` の `整理済み` 件数
- 最終履歴日時

### 最近の処理履歴

`修正履歴` の最新10件を新しい順に表示します。

## 安全設計

- Google Places APIを呼び出さない
- 元DBを変更しない
- `修正候補` を変更しない
- `要確認` を変更しない
- `重複候補` を変更しない
- `新規追加候補` を変更しない
- `新規施設分類候補` を変更しない
- `修正履歴` を変更しない
- `閉業除外履歴` を変更しない
- `重複整理履歴` を変更しない
- 書き換えるのは `統合ダッシュボード` だけ
- 対象シートが存在しなくても0件として表示

`統合ダッシュボード` は自動生成シートです。ユーザー独自のメモやデータは保存しないでください。⑲実行時に内容と書式を再生成します。

## 自己診断

`runHotelDbV2DashboardTests()`

状態分類、件数集計、全体状態、優先アクション、ワークフロー定義、数値変換、安全契約などを外部API・シート書換なしで検証します。

## コピー版UIテスト

`宿泊施設DB_PR13_⑧反映テスト` 専用です。

1. `setupHotelDbV2DashboardUiTest()`
2. `testHotelDbV2DashboardUiTest()`
3. `cleanupHotelDbV2DashboardUiTest()`

setup時に既存の `統合ダッシュボード` があれば退避し、実データから新しいダッシュボードを生成します。

UIテストでは、タイトル、5セクション、5ワークフロー件数を確認し、さらに9つの運用シートについて内容・数式のSHA-256ハッシュをsetup前後で比較します。

監視対象:

- 修正候補
- 要確認
- 修正履歴
- 重複候補
- 実行サマリー
- 新規追加候補
- 新規施設分類候補
- 閉業除外履歴
- 重複整理履歴

1つでも内容が変化した場合、UIテストは失敗します。

cleanupではテストで生成したダッシュボードを削除し、元のダッシュボードがあった場合は復元します。

## PR #21 の対象外

- ダッシュボードからの承認操作
- 自動バッチ実行
- Google Places API呼び出し
- メール・Slack通知
- 定期実行
- ユーザー権限管理
- 複数スプレッドシート横断集計

これらは製品化フェーズの後続PRで検討します。
