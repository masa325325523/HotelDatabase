# Ver2.0 修正候補の自動仕分け

`V2CorrectionTriage.gs` は、`修正候補` シートを人が確認しやすいように自動仕分けします。

## 安全方針

- B列 `状態` は自動変更しません。
- 元シートの郵便番号・市区町村・住所・施設名は変更しません。
- `⑧ 承認済み修正候補を反映` は、従来どおり人が B列を `承認` にした行だけを対象にします。
- 自動仕分けは `推奨判定`、`自動判定理由`、`信頼度` の3列だけを書き込みます。

## 推奨判定

### 承認候補

人が優先的に確認する修正候補です。自動承認ではありません。

現行ルールでは主に次を対象にします。

- Google営業状態が `営業中`
- Place IDあり
- 一致スコア90点以上
- 差分が1項目だけ
- 住所差分の場合、施設名・郵便番号・市区町村が一致
- 建物名・階・部屋番号差の疑いがない
- 郵便番号差分の場合、施設名・市区町村・住所が一致し、一致スコア95点以上

### 却下候補

元データを維持する可能性が高い候補です。自動却下ではありません。

現行ルールでは、住所・郵便番号・市区町村が一致し、施設名の差が次のような付加表記だけと判定できる場合を対象にします。

- 地域名の追加（例: `北川旅館` → `鳥取 北川旅館`）
- 施設種別・業態語の追加（例: `常天` → `寿司 旅館 常天`）

### 要人確認

自動判断を避ける候補です。

- 一致スコア90点未満
- Place IDなし
- Google営業状態が営業中以外
- 複数項目に差分
- 市区町村差分
- 建物名・階・部屋番号の違いが疑われる
- 施設名差分が単純な付加表記とは判断できない

## 使い方

1. Apps Scriptへ `V2CorrectionTriage.gs` を追加する。
2. 最新の `Code.gs` に更新する。
3. `runHotelDbV2CorrectionTriageTests` を実行し、`成功件数: 8件 / 失敗件数: 0件` を確認する。
4. スプレッドシートを再読み込みする。
5. `宿泊施設DB → ⑨ 修正候補を自動仕分け` を実行する。
6. `修正候補` シート右端の `推奨判定`、`自動判定理由`、`信頼度` を確認する。
7. `要人確認` を優先して人が調査する。
8. `承認候補` も最終確認後にのみ、B列 `状態` を手動で `承認` にする。
9. `却下候補` は確認後、必要ならB列 `状態` を手動で `却下` にする。
10. 全件確認後にのみ `⑧ 承認済み修正候補を反映` を実行する。

## 再実行

新しい50件バッチを実行したり、Google候補が更新された場合は、`⑨ 修正候補を自動仕分け` をもう一度実行してください。

既存のB列 `状態` は変更しません。
