# PR #18 追加済み新規施設の宿泊分類・備考を安全に確定

## 目的
PR #17で安全に追加した新規施設は、Google情報だけでは法的な宿泊分類を確定できないため `宿泊分類` を空欄にしています。
PR #18では、人が公的資料・許可情報などを確認したうえで分類を確定できる補助フローを追加します。

## ⑮ 追加済み新規施設の分類候補を作成
- `新規追加候補` で `追加済み` の施設を確認
- 元DBで `宿泊分類` が空欄かつ備考に `宿泊分類要確認` が残っている施設だけを対象
- `新規施設分類候補` シートへ整理
- Googleタイプから `参考分類`・`参考理由`・`参考信頼度` を表示
- 参考分類は自動確定ではない
- 元DBは変更しない

## ⑯ 承認済み宿泊分類・備考を安全に反映
人が `新規施設分類候補` の以下を入力・確認します。
- `確定宿泊分類`
- `確定備考`（任意）
- `状態` を `承認`

反映直前に以下を再確認します。
- 元シート名・ID・行
- Place ID
- 施設名
- 住所
- 現在宿泊分類
- 現在備考
- 元DBの宿泊分類がまだ空欄であること

1つでも変化があれば `要再確認` として反映しません。

## 備考の扱い
既存備考は保持します。
`宿泊分類要確認` の文字だけを外し、`宿泊分類確認済` と人が入力した確定備考を追記します。

## 自動で行わないこと
- Googleタイプだけで宿泊分類を確定しない
- 既存宿泊分類を上書きしない
- 施設名・住所・Place IDを変更しない
- 元行を削除しない

## 自己診断
Apps Scriptで以下を実行します。

`runHotelDbV2NewFacilityClassificationFinalizeTests`

期待結果:
- 成功 24件
- 失敗 0件
- Googleタイプからの自動確定なし
- 既存分類の上書きなし
- 施設名・住所・Place ID・備考変更時の反映なし
- 元データ変更なし

自己診断後、コピー版で⑮→人の承認→⑯のUI実地テストを行ってからマージします。
