# PR #20 承認済み重複候補の安全な整理

## 目的

PR #12の安全な重複仕分けを、人の承認後の実整理までつなげます。

- 施設名・住所が正規化後に完全一致する `重複濃厚` だけが整理候補
- 人が `残す行` に行1または行2の行番号を明示する
- `状態=承認` の候補だけを⑱で処理する
- 別部屋・別階・同住所別名・同名別住所など `要人確認` は整理しない
- 物理行削除は行わず、アーカイブ後に除外行の内容だけ `clearContent()` する

## なぜ「残す行」を自動選択しないか

同じ施設の重複行でも、片方だけに宿泊分類、備考、Place ID、公式サイトなどの情報が入っていることがあります。

コードが行番号や入力項目数だけで残す行を決めると、重要情報を失う可能性があります。そのためPR #20は、どちらを残すかを人が明示するまで実行しません。

## ⑩の変更

既存のPR #12判定ロジック自体は変更しません。

⑩実行時に `重複濃厚` の候補だけ、行1・行2の完全な行スナップショットからSHA-256ハッシュを作成します。

追加監査列:

- 整理候補作成日時
- 行1ハッシュ
- 行2ハッシュ
- 整理スナップショット状態
- 残す行
- 除外予定行
- 整理処理日時
- 整理履歴キー
- 整理結果

承認済み候補について、スナップショットが新規作成または変更になった場合は、安全のため `要再確認` に戻します。

## ⑱の処理条件

次をすべて満たした候補だけ処理します。

1. `状態 = 承認`
2. `推奨判定 = 重複濃厚`
3. 整理スナップショット状態が `作成済み`
4. 行1ハッシュ・行2ハッシュが存在
5. `残す行` が行1または行2のどちらか
6. 元シートID・元シート名・行1・行2が現在も存在
7. 行1/行2の現在ハッシュが候補作成時と一致
8. 行1/行2の施設名・住所が候補作成時と一致
9. 現在も両行の施設名・住所が正規化後に完全一致
10. 両行に異なるPlace IDが入っていない
11. 情報損失チェックに合格

## 情報損失チェック

除外する行の全列を、残す行と比較します。

整理しない例:

- 除外行だけに備考がある
- 除外行だけにPlace IDがある
- 宿泊分類が異なる
- 公式サイト等の値が競合する
- 除外行だけに数式がある
- 両行の数式が異なる

安全な例:

- 全列が同一
- 残す行の方だけに追加情報があり、除外行には独自情報がない
- 施設名の空白・全角半角差だけ
- 住所の丁目番地表記差だけ
- 郵便番号のハイフン差だけ

独自情報がある場合は自動コピー・自動マージしません。人が元DBを確認し、必要な情報を残す行へ整理した後、⑩で新しいスナップショットを作り直して再承認します。

## 重複整理履歴

新しい `重複整理履歴` シートへ、整理前に両行を完全保存します。

主な保存内容:

- 元シート・元シートID
- 重複キー
- 行1/行2と各ハッシュ
- 残す行・除外行
- 推奨判定・理由・信頼度
- 元見出し
- 残す行の値・数式
- 除外行の値・数式
- 情報損失チェック結果

履歴の読み戻し検証に成功してから、除外行だけを `clearContent()` します。

`deleteRow()` / `deleteRows()` は使用しません。下の施設の行番号は変わりません。

## ロールバック

除外行のクリア後に処理エラーが起きた場合は、アーカイブ前に保持した値・数式を復元し、元ハッシュへ戻ったことを確認します。

## 関連候補

整理成功後、除外された元行を参照する未完了の修正候補・要確認・重複候補・新規施設分類候補は `要再確認` にします。

完了済み履歴は変更しません。

## 自己診断

`runHotelDbV2ApprovedDuplicateConsolidationTests()`

- 承認条件
- 残す行の明示
- 別部屋・別施設保護
- Place ID競合保護
- 情報損失チェック
- 数式保護
- 整理キー
- 物理行削除禁止

を外部API・本番データ変更なしで検証します。

## コピー版UIテスト

対象: `宿泊施設DB_PR13_⑧反映テスト`

1. `setupHotelDbV2ApprovedDuplicateConsolidationUiTest()`
2. メニュー⑩ `重複候補を自動仕分け`
3. `testHotelDbV2ApprovedDuplicateConsolidationUiTest()`
4. 結果を確認
5. `cleanupHotelDbV2ApprovedDuplicateConsolidationUiTest()`

期待値:

- 承認対象 4
- 整理済み 1
- 要再確認 3
- エラー 0
- 情報損失ケース保護 成功
- 別部屋保護 成功
- 下行番号保持 成功
- 重複整理履歴は正常1件のみ

## 対象外

PR #20では次を行いません。

- `要人確認` 候補の自動整理
- 別部屋・別階の自動統合
- 異なる施設名の自動統合
- 残す行の自動選択
- 除外行から残す行への自動データマージ
- 物理行削除
- 宿泊分類の自動変更
