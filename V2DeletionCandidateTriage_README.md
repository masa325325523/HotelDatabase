# PR #15 閉業・未検出施設の安全な削除候補仕分け

## 目的
`要確認`シートのうち、閉業・Google候補なし・一時休業・開業予定を安全側に仕分けします。

このPRは削除を実行しません。元データ、Place ID、既存の`状態`列も変更しません。

## 追加列
- `削除推奨判定`
- `削除判定理由`
- `削除信頼度`

## 判定
### 削除候補有力
次をすべて満たすときだけです。
- 理由が`閉業`
- 候補Place IDあり
- Google営業状態が`閉業`
- 一致スコアが既存の自動採用基準75点以上

それでも自動削除はしません。人の最終確認が必要です。

### 要人確認
- `Google候補なし`
- 閉業だがPlace IDなし
- 閉業だが75点未満
- 閉業理由とGoogle営業状態が不整合

### 削除非推奨
- 一時休業
- 開業予定

### 対象外
- APIエラー
- PR #14で扱う低スコア案件
- `状態`が`未確認`ではない行

## 自己診断
Apps Scriptで次を実行します。

`runHotelDbV2DeletionCandidateTriageTests`

期待結果:
- 成功16件
- 失敗0件
- Google候補なしだけで削除候補化しない
- 一時休業・開業予定を削除候補化しない
- 自動削除なし
- 元データ・Place ID・状態列の変更なし

## 安全なUI実地テスト
コピー版`宿泊施設DB_PR13_⑧反映テスト`でのみ実行します。

1. `V2DeletionCandidateUiTest.gs`をApps Scriptへ追加する。
2. `setupHotelDbV2DeletionCandidateUiTest`を実行する。
3. 通常の`要確認`が`PR15_要確認退避`へ退避され、7件のテスト用`要確認`が作られることを確認する。
4. メニュー⑫`閉業・未検出を削除候補に仕分け`を実行する。
5. 期待結果を確認する。
   - 確認件数: 7
   - 削除候補有力: 1
   - 削除非推奨: 2
   - 要人確認: 3
   - 対象外: 1
6. `削除推奨判定`・`削除判定理由`・`削除信頼度`を目視確認する。
7. `cleanupHotelDbV2DeletionCandidateUiTest`を実行する。
8. テスト用`要確認`が削除され、退避した通常の`要確認`が復元されることを確認する。

## 安全性
- `deleteRow`なし
- 元シートの行削除なし
- 元データ上書きなし
- Place ID上書きなし
- `状態`の自動変更なし
- Google検索失敗だけで削除候補にしない

承認済み削除を実際に反映する機能は、このPRの対象外です。
