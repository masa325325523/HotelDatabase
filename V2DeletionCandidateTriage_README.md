# PR #15 閉業・未検出施設の安全な削除候補仕分け

## 目的

`要確認` シートにある閉業・未検出等の行を、元データを変更せずに安全側へ仕分けします。

追加する列は次の3列だけです。

- `削除推奨判定`
- `削除判定理由`
- `削除信頼度`

## 判定

- `削除候補有力`
  - `理由=閉業`
  - 候補Place IDあり
  - Google営業状態=`閉業`
  - 一致スコアが本体の自動採用基準75点以上
  - **それでも自動削除はしない**
- `要人確認`
  - Google候補なし
  - 閉業でもPlace IDなし
  - 閉業でも一致スコア75点未満
  - 理由とGoogle営業状態が不整合
- `削除非推奨`
  - 一時休業
  - 開業予定
- `対象外`
  - APIエラー
  - 低スコア照合などPR #14側の対象
  - すでに`未確認`以外の状態になっている行

## 安全方針

- 元シートの行は削除しない
- 元データは変更しない
- Place IDは変更しない
- `要確認`の既存`状態`列は変更しない
- Google候補なしだけでは施設不存在と断定しない
- 一時休業・開業予定は削除候補にしない
- `削除候補有力`も人による最終確認が必要

## 手動検証

1. Apps Scriptに `V2DeletionCandidateTriage.gs` を追加する。
2. PR #15版の `Code.gs` に更新する。
3. `runHotelDbV2DeletionCandidateTriageTests` を実行する。
4. 16件成功・0件失敗を確認する。
5. コピー版スプレッドシートの`要確認`で、閉業・未検出を含むデータに対してメニュー⑫を実行する。
6. `削除候補有力`が高一致の恒久閉業だけになっていることを確認する。
7. Google候補なしが`要人確認`、一時休業・開業予定が`削除非推奨`になっていることを確認する。
8. 元データ・Place ID・`状態`列が変わっていないことを確認する。

## 重要

PR #15は削除の実行機能ではありません。承認済み削除を実際に元シートへ反映する機能は、このPRには含めません。
