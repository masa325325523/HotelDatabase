# PR #22 初期セットアップ・設定画面

## 目的

宿泊施設DB Ver2.0を初めて利用する人が、Apps Scriptの「プロジェクトの設定」を直接操作しなくても、必要な初期状態を1画面で確認・準備できるようにします。

このPRは製品化フェーズのUI改善です。既存の①〜⑲の照合・候補作成・承認反映ロジックは変更しません。

## メニュー

`宿泊施設DB > ⚙ 初期セットアップ・設定`

モーダル画面で次を確認・操作できます。

1. Google Places APIキーの設定状態
2. APIキーの新規保存・変更
3. 保存済みAPIキーの削除（確認ダイアログあり）
4. Google Places API接続テスト
5. 現在開いているシートが元データとして使用可能か
6. コア運用シート5種類の準備状態
7. 不足コアシートだけの新規作成
8. 次に行うべき操作の案内

## APIキーの安全設計

APIキーは既存仕様どおり Script Properties の次のキーにだけ保存します。

`GOOGLE_PLACES_API_KEY`

安全条件:

- 保存済みAPIキー本体を設定画面へ返さない
- 保存済みAPIキーの末尾文字なども表示しない
- APIキーをスプレッドシートへ書かない
- APIキーを修正履歴・実行サマリーへ書かない
- APIキーを `console.log` しない
- APIエラー文に万一キー文字列が含まれた場合は `[API_KEY]` へ置換する
- 自己診断は実Script Propertiesを変更せず、偽のストアで保存/削除処理を検証する
- コピー版UIテストでは実APIキーのSHA-256ハッシュだけを保持し、前後不変を確認する

## APIキー入力検証

過度にGoogleのキー形式へ依存しないよう、固定prefix（`AIza`等）は要求しません。

以下のみ検証します。

- 空欄ではない
- 20〜200文字
- キー途中に空白・改行がない
- 前後空白はtrimする

正しいキーかどうかは、利用者が明示的に「接続テスト」を押したときにGoogle Places APIへ問い合わせて確認します。

## 元データ診断

現在のアクティブシートについて、次を確認します。

### 必須

- `施設名` 列
- `住所` または `市区町村` 列

### 推奨

- `郵便番号`
- `宿泊分類`
- `備考`

次の管理・出力用シートは元データとして案内しません。

- 修正候補
- 要確認
- 修正履歴
- 重複候補
- 実行サマリー
- 新規追加候補
- 新規施設分類候補
- 閉業除外履歴
- 重複整理履歴
- 統合ダッシュボード

## コア運用シート準備

対象は次の5シートです。

- 修正候補
- 要確認
- 修正履歴
- 重複候補
- 実行サマリー

「運用シートを準備」を押した場合:

- 存在しないシートだけ新規作成
- 新規シートに既存の正式ヘッダを設定
- 1行目を固定
- 既存シートは変更しない
- 既存シートが空、または基本ヘッダと不整合なら `要確認` と表示するだけ
- 既存シートの見出しを自動修正しない
- 作成途中で失敗した場合、その処理で新規作成したシートだけ可能な範囲でロールバックする

PR #9以降で追加された仕分け列など、正式基本ヘッダの末尾に追加された列は許容します。

## 接続テスト

設定画面の「接続テスト」は既存の `hotelDbV2ConnectionTest_()` を利用します。

- 自動では実行しない
- 設定画面を開いただけではAPI呼び出ししない
- 利用者がボタンを押したときだけ1回の接続確認を開始する
- 成功時は確認施設名・住所・営業状態を表示する
- APIキー本体は表示しない

## 自己診断

`runHotelDbV2SetupTests()`

78項目を検証します。

内訳: `11 + 6 + 15 + 11 + 8 + 10 + 12 + 5 = 78`

- APIキー入力検証
- 偽Script Propertiesへの安全な保存/削除
- 元データ必須列・推奨列判定
- 管理シートの元データ誤認防止
- コアシート基本ヘッダ互換判定
- 次アクション生成
- コアシート定義
- 固定安全契約

自己診断では次を行いません。

- Google Places API呼び出し
- 実Script Properties変更
- 元DB変更
- 候補・履歴変更

## コピー版UIテスト

対象スプレッドシート名に `PR13` と `⑧反映テスト` の両方が含まれる場合だけ実行可能です。

1. `setupHotelDbV2SetupUiTest()`
2. `testHotelDbV2SetupUiTest()`
3. 設定画面を手動で開いて目視確認
4. 必要なら設定画面の「接続テスト」を手動実行
5. `cleanupHotelDbV2SetupUiTest()`

### UIテストで確認すること

- 元データとして利用可能なシートを安全に選択
- status応答に保存済みAPIキー本体が含まれない
- HTMLが正常に読み込める
- APIキー入力欄が `password` 型
- 保存済みキーを再表示しない説明がある
- 不足していたコアシートだけを新規作成
- 既存運用シートの値・数式SHA-256が不変
- Script Properties APIキーのSHA-256が不変
- 自動Google Places API呼び出しなし

cleanupでは、UIテスト前に存在しなかったのにテストで作成されたコアシートだけを削除し、元の状態へ戻します。

## Out of Scope

PR #22では次を行いません。

- APIキーの自動発行
- Google Cloud Consoleの設定変更
- Google Places APIの有効化
- 課金設定
- APIキー制限の自動設定
- 既存運用シートの自動修復
- 元DB見出しの自動変更
- 宿泊分類の自動確定
- 候補の自動承認
- 元DBの自動修正・自動削除

## マージ条件

- PR #22自己診断 78/78
- コピー版UIテスト成功
- 設定画面の目視確認
- APIキー本体が画面へ再表示されないことを確認
- 既存運用シートのハッシュ不変
- UIテスト中のScript Properties APIキー不変
- 不足コアシートだけが作成されることを確認
- 設定画面を開くだけではGoogle Places APIを呼ばない
- cleanupでコピー版を元の状態へ復元
- 明示的な「マージして！」があるまでマージしない
