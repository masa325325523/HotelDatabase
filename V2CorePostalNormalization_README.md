# PR #7 本体郵便番号正規化

## 目的
PR #6で鳥取の実データを使って安全性を確認した郵便番号のNFKC正規化を、Google Places照合の共通関数 `hotelDbV2NormalizePostalCode_` に組み込みます。

これにより、今後の本番バッチでは次の表記を同じ郵便番号として扱います。

- `680-0907`
- `680-090７`
- `６８０－０９０７`
- `〒 ６８０－０９０７`
- `6800907`
- `680 0907`

## 効果が及ぶ処理
共通関数を直すため、次の3か所へ同じ改善が効きます。

1. Google Places検索クエリ
2. 一致スコア計算の郵便番号加点
3. `修正候補`を作る際の郵便番号差分判定

元シートの郵便番号文字列は変更しません。

## 安全性
- 元シートの郵便番号・住所・施設名を変更しない
- B列 `状態` を変更しない
- 7桁に正規化できない値は従来どおり空文字扱い
- 実際に異なる7桁の郵便番号は同一扱いしない
- 郵便番号以外の施設名・住所等の差分は消さない

## テスト
Apps Scriptで `runHotelDbV2CorePostalNormalizationTests` を実行します。

期待結果:

- 成功件数: 14件
- 失敗件数: 0件
- 状態列の自動変更: なし

テスト内容:

- ASCII標準
- 一部全角数字
- 全角数字・全角ハイフン
- `〒`、空白、ハイフンなし
- 無効な桁数
- 検索クエリへ正規化済み郵便番号が入る
- 一致スコアで郵便番号10点を正しく加点する
- 全角半角だけでは `郵便番号` 差分を作らない
- 実際に違う郵便番号は差分として残る
- 施設名差分など他の差分は残る

## 手動検証
1. Apps Scriptの `V2ConfigApi.gs` をPR #7版へ置き換える。
2. `V2CorePostalNormalizationTests.gs` を追加する。
3. `runHotelDbV2CorePostalNormalizationTests` を実行し14/14成功を確認する。
4. まだ `⑧ 承認済み修正候補を反映` は実行しない。
5. その後、既知の鳥取データ数件で再照合し、全角郵便番号だけを理由とする修正候補が新規発生しないことを確認する。
