# PR #16 営業中の新規宿泊施設を探索する新規追加候補機能

## 目的
自治体公開データを元に作成した既存DBだけでは拾えていない可能性のある宿泊施設を、Google Places API (New) から安全に探索する。

このPRは**候補作成まで**です。元データへ自動追加しません。

## メニュー
`⑬ 営業中の新規宿泊施設を探索`

元データシートで、探索したい市区町村の施設データ行を1行選択して実行する。
選択行の`市区町村`を探索対象にする。

## 検索カテゴリ
Google Placesの宿泊系Place Typeを9カテゴリ検索する。

- hotel
- japanese_inn
- budget_japanese_inn
- guest_house
- hostel
- bed_and_breakfast
- private_guest_room
- cottage
- inn

各カテゴリは1ページ20件、最大3ページまで取得する。Place IDで重複をまとめる。

## 新規追加候補に残す条件
- Place IDあり
- Google営業状態が`OPERATIONAL`
- 候補住所が対象市区町村内
- 既存DBに同一Place IDなし
- 既存の施設名+住所の完全一致なし

## `新規候補有力`
上記を満たし、既存DBの名称・住所との強い類似もない施設。

**自動追加はしない。人が最終確認する。**

## `要人確認`
次のような場合は、新規と断定しない。

- 既存DBと住所が同じだが名称が違う
- 既存DBと名称が同じだが住所が違う
- 施設名と住所がともに高類似
- 同一建物・別室・リブランド・移転などの可能性がある

## 候補から除外
- 既存Place ID一致
- 既存施設名+住所一致
- 閉業
- 一時休業
- 開業予定
- 営業状態不明
- 対象市区町村外
- Place IDなし

## 出力シート
`新規追加候補`

追加する主な列:
- 候補Place ID
- 候補施設名
- 候補住所
- 候補郵便番号
- 候補市区町村
- 営業状態
- Googleタイプ
- 電話番号
- 公式サイト
- 評価
- 口コミ数
- Google Maps URL
- 既存照合
- 推奨判定
- 自動判定理由
- 信頼度
- 発見日

## 安全保証
- 元データ行を追加しない
- 元データを修正しない
- 元データを削除しない
- 既存Place IDを書き換えない
- `新規追加候補`の状態を自動承認しない
- Googleの営業中だけを根拠に自動登録しない
- Google検索だけで全国の宿泊施設を完全網羅したとは扱わない

## 自己診断
Apps Scriptで次を実行する。

`runHotelDbV2NewFacilityDiscoveryTests`

期待値:
- 成功件数: 18件
- 失敗件数: 0件
- 既存Place IDの重複追加: なし
- 営業中以外の新規候補化: なし
- 自治体外施設の新規候補化: なし
- 元データへの自動追加・自動変更: なし

自己診断ではGoogle API検索を行わない。

## 自己診断成功後の実地テスト
コピー版スプレッドシートで行う。

1. 元データシートを開く。
2. テストしたい市区町村のデータ行を1行選択する。
3. メニュー⑬を実行する。
4. 完了画面の件数を確認する。
5. `新規追加候補`で、候補Place ID・住所・営業状態・既存照合・推奨判定を確認する。
6. 既存施設が`新規候補有力`に混ざっていないことを確認する。
7. 閉業・自治体外・同一建物などの危険ケースが自動追加されていないことを確認する。

## このPRの対象外
- 承認済み新規候補を元DBへ追加する処理
- 自治体許可データとの自動照合
- Google検索だけによる網羅性保証
- 宿泊営業許可区分の自動確定

承認済み候補の安全な追加反映は、別PRで実装する。
