# PR #17 承認済み新規追加候補の安全追加

`新規追加候補`で状態が`承認`の行だけを対象に、探索元シートへ新規1行を追加する機能です。

追加直前にGoogle Placesを再取得し、営業中・対象自治体内・Place ID一致・施設名/住所/郵便番号が候補作成時から変わっていないことを確認します。さらに既存DBを再照合し、同じPlace IDや名称/住所の一致・強い類似があれば追加せず`要再確認`にします。

宿泊分類はGoogleだけでは確定しないため自動入力しません。備考には`PR17承認新規追加／宿泊分類要確認`を残します。既存行の上書き・削除は行いません。

自己診断は`runHotelDbV2ApprovedNewFacilityInsertTests`を実行します。期待値は成功20件・失敗0件です。自己診断後、コピー版で実地追加テストを行ってからマージ判断します。
