# PR #6: 郵便番号の全角・半角表記差を安全に再判定

## 目的
自治体データに含まれる全角数字・全角ハイフン・空白・〒記号などにより、Googleと同じ郵便番号なのに「修正候補」と判定されるケースを減らします。

例:
- `680-090７`
- `６８０－０９０７`
- `〒 6800907`
- `680 0907`

これらは比較用にはすべて `680-0907` として扱います。

## 安全設計
- B列「状態」は変更しません。
- 元郵便番号・元住所・元施設名などの元データは変更しません。
- 既存の `承認候補` / `却下候補` / `対象外` は変更しません。
- 現在 `要人確認` または未分類の行だけを対象にします。
- 「差分」に `郵便番号` が含まれる行だけを対象にします。
- 正規化後の元郵便番号とGoogle郵便番号が完全一致した場合だけ、郵便番号差分を表記差として除外します。
- 郵便番号以外の差分が残る場合は、PR #4の既存仕分けルールへ戻して再判定します。
- 残る差分が危険・低スコアなら `要人確認` のままです。

## 鳥取で想定する主な対象
- `680-090７` → `680-0907`
- `689-122８` → `689-1228`
- `682-002３` → `682-0023`
- `689-340１` → `689-3401`（施設名差分も残るため、郵便番号だけ解消して施設名は人確認）

## テスト
Apps Scriptで次を実行します。

`runHotelDbV2PostalCodeNormalizationTests`

期待結果:
- 成功件数: 10件
- 失敗件数: 0件
- 状態列の自動変更: なし

テスト内容:
1. ASCII標準
2. 末尾だけ全角
3. すべて全角
4. 〒と空白付き
5. ハイフンなし
6. 空白区切り
7. 異なる郵便番号を区別
8. 桁不足を無効扱い
9. 郵便番号表記差だけなら却下候補
10. 郵便番号を解消しても非単純な施設名差が残れば要人確認

## 実データ検証
テスト成功後、次を実行します。

`runHotelDbV2PostalCodeNormalizationRefinement`

完了画面で以下を確認します。
- 確認件数
- 郵便番号表記差を解消
- 却下候補へ変更
- 承認候補へ変更
- 要人確認のまま
- 変更なし
- B列「状態」は変更していない

## スコープ
このPRはまず既存の修正候補を安全に再判定する段階です。鳥取実データで効果と誤判定がないことを確認後、同じNFKC正規化をコアの `hotelDbV2NormalizePostalCode_` に統合する後続変更を検討します。
