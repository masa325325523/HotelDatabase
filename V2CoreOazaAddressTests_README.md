# PR #11: 大字表記の住所正規化を本体へ組み込む

## 目的

自治体データの `大字` の有無だけで、一致スコアが下がったり不要な修正候補が発生したりするのを防ぎます。

例:

- 元: `鳥取県日野郡 / 江府町大字江尾2064 / 門脇旅館`
- Google: `鳥取県江府町 / 日野郡江府町江尾2064 / 門脇旅館`

## 変更内容

### 1. 本体住所正規化

`V2ConfigApi.gs` の `hotelDbV2NormalizeAddress_` で、比較時だけ `大字` を除去します。

元データやGoogle住所の表示値は書き換えません。

この共通正規化は次に反映されます。

- 住所同一判定
- 一致スコア計算
- 修正候補の差分判定

### 2. 郡・町村判定の安全な互換処理

`V2CountyMunicipalityTriage.gs` の通常基準 `90点以上` は維持します。

ただし、次をすべて満たし、片側だけに `大字` がある場合に限り、既存の古い候補データでも `AUTO_ACCEPT_SCORE`（現在75点）以上なら同一所在地として扱えます。

- Place IDあり
- 営業中
- 差分が `市区町村・住所` だけ
- 郵便番号一致
- 施設名一致
- 郡→町村の対応が一致
- `大字` を除いた結合住所が完全一致
- 番地まで一致

番地違い、郵便番号違い、施設名違い、75点未満は通しません。

## 回帰テスト

Apps Scriptへ次を反映します。

1. PR #11版 `V2ConfigApi.gs` に入れ替え
2. PR #11版 `V2CountyMunicipalityTriage.gs` に入れ替え
3. 新規 `V2CoreOazaAddressTests.gs` を追加

その後、次を実行します。

`runHotelDbV2CoreOazaAddressTests`

期待値:

- 成功件数: 12件
- 失敗件数: 0件
- 状態列の自動変更: なし

さらに既存の

`runHotelDbV2CountyMunicipalityTriageTests`

も実行し、期待値:

- 成功件数: 13件
- 失敗件数: 0件
- 状態列の自動変更: なし

を確認します。

## 実データ検証

テスト成功後、門脇旅館1件だけを新しいテストシートへコピーし、Google出力列を空にして `③ Ver2.0 先頭3件テスト` を実行します。

期待する結果:

- 営業中: 1
- エラー: 0
- 一致スコア: 93前後（少なくとも90以上）
- `大字` の有無だけを理由に修正候補を作らない
- 元データの `江府町大字江尾2064` は変更しない

## 安全方針

- B列 `状態` は自動変更しません。
- 元データを自動修正しません。
- 建物名・階・部屋番号差を消しません。
- 番地違いを同一扱いしません。
- ⑧「承認済み修正候補を反映」はPR #11検証中に実行しません。
