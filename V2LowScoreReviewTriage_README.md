# PR #14 低スコア要確認の安全な自動仕分け

## 目的

`要確認`シートのうち、一致スコアが本体の自動採用基準 75 点未満で止まった候補を、確認しやすいように安全側へ仕分けします。

この機能は **本体の75点基準を下げません**。また、元データ、Place ID、`状態`列を変更しません。

## 追加する推奨判定

- `同一施設有力`: 住所・市区町村が強く一致し、施設名にも十分な一致証拠がある、または同住所で50点以上の候補
- `誤候補有力`: 住所・市区町村・施設名の一致証拠が弱く、スコアも低い候補
- `要人確認`: 別館・別邸・新館・支店・Cafe/Restaurant・階・部屋番号など安全に自動判断できない候補
- `対象外`: 低スコア理由ではない、75点以上、すでに確認済み等

## 安全ルール

- `AUTO_ACCEPT_SCORE = 75` は変更しない
- 元データを変更しない
- 候補Place IDを元シートへ保存しない
- `要確認`シートの`状態`列を変更しない
- 自動削除しない
- `同一施設有力`でも自動採用・自動反映しない
- `誤候補有力`でも自動削除しない
- 別館・別邸・新館・本館・離れ・支店・駅前・ANNEX・Cafe・Restaurant等の構造差は人確認へ残す
- 階・部屋番号差の可能性がある住所は人確認へ残す

## Apps Script への反映

1. `V2LowScoreReviewTriage.gs` をApps Scriptへ追加します。
2. `Code.gs` をPR #14版へ置き換えます。
3. スプレッドシートを再読み込みし、メニューに `⑪ 低スコア要確認を自動仕分け` が追加されたことを確認します。

## 自己診断

Apps Scriptから次を実行します。

`runHotelDbV2LowScoreReviewTriageTests`

期待結果:

- 成功件数: 20件
- 失敗件数: 0件
- 状態列の自動変更: なし
- 元データ・Place IDの自動変更: なし
- 本体75点基準の変更: なし

テストには以下を含みます。

- 同住所・同名
- 日本語施設名と英字施設名
- 施設名の包含表記
- 別館 / ANNEX / Cafe 差分
- 同一建物の別部屋
- 別自治体・別住所・別施設名
- 低スコアの明確な誤候補
- 同名だが別住所
- 営業中ではない候補
- Place IDや住所が欠ける候補
- 75点以上や低スコア理由以外の対象外行

## 大阪50件での実地確認

自己診断20件が成功した後、現在の大阪テストの`要確認`シートで `⑪ 低スコア要確認を自動仕分け` を実行します。

確認すること:

1. `推奨判定`、`自動判定理由`、`信頼度`の3列だけが追加・更新されること
2. B列`状態`が変わっていないこと
3. 元の大阪データが変わっていないこと
4. `同一施設有力`に、住所一致が強い日本語/英字表記差などが入ること
5. `誤候補有力`に、低スコアかつ住所・施設名の一致証拠が弱い候補が入ること
6. 別館・別邸・新館・部屋・階など危険な差は`要人確認`のまま残ること

実地確認が終わるまで、判定結果を元データへ自動反映しません。
