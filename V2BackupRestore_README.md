# PR #24 バックアップ・復元

## 目的

宿泊施設DB Ver2.0の製品化フェーズとして、誤操作・反映ミス・データ破損に備えたバックアップと安全な復元導線を追加します。

## 利用者向け入口

メニュー上部に次を追加します。

- `💾 バックアップ・復元`

## バックアップ方式

- 現在のGoogleスプレッドシートをDrive上の専用フォルダ `宿泊施設DB_Backups` へ完全コピーします。
- コピー後、元ファイルとバックアップの全シートについて値・数式をブロック単位でSHA-256化し、全体フィンガープリントを比較します。
- 一致したコピーだけを `検証済みバックアップ` としてファイル説明欄の管理メタデータに登録します。
- 検証失敗で新規作成されたコピーは誤利用防止のためゴミ箱へ移します。
- Google Places APIは呼びません。

## 復元方式

- 現在の運用ファイルへ直接書き戻しません。
- 選択したバックアップが専用バックアップフォルダ内にあり、元スプレッドシートID・形式・フィンガープリントが正しいことを再確認します。
- バックアップ作成後に内容が変更されていた場合は停止します。
- 問題がなければ別ファイル `【復元候補】...` を作成します。
- 復元候補のフィンガープリントがバックアップと一致したことを確認してからURLを案内します。
- 利用者は復元候補を開いて目視確認し、必要な場合だけ運用先を切り替えます。

## セキュリティ

Google公式ドキュメントでは、Google SheetsをコピーするとそのスプレッドシートにバインドされたApps Scriptもコピーされると説明されています。

そのためバックアップ・復元候補は機密ファイルとして扱い、外部共有しないでください。

この機能自身は次を守ります。

- APIキー本体を画面へ返さない
- APIキー本体をバックアップ管理メタデータへ書かない
- APIキー本体をログへ出さない
- APIキー保存先 `GOOGLE_PLACES_API_KEY` を変更しない
- Google Places APIを呼ばない

## Drive保存先

初回バックアップ時に `宿泊施設DB_Backups` フォルダを作成し、そのフォルダIDだけをScript Propertiesの `HOTEL_DB_V2_BACKUP_FOLDER_ID` へ保存します。

APIキーのScript Propertyは変更しません。

## ヘルスチェック連携

PR #24で製品全体ヘルスチェックも更新します。

- 機能入口: 21 → 22
- 安全回帰入口: 7 → 8
- `💾 バックアップ・復元` の実行入口を確認
- `PR #24 バックアップ・復元自己診断` の入口を確認

## 自己診断

`runHotelDbV2BackupRestoreTests()`

現在の定義では68項目を確認します。自己診断ではDriveコピーを作りません。

- 定数・安全契約
- ファイル名生成
- SHA-256
- メタデータ生成・検証・解析
- 実スプレッドシートの読み取り専用フィンガープリント
- 元DB・候補・履歴不変
- APIキー不変
- HTML安全性
- ヘルスチェック22入口 / 8安全回帰連携

## コピー版UIテスト

対象: `宿泊施設DB_PR13_⑧反映テスト`

1. `runHotelDbV2BackupRestoreTests()`
2. `setupHotelDbV2BackupRestoreUiTest()`
3. `testHotelDbV2BackupRestoreUiTest()`
4. スプレッドシートを再読み込み
5. `宿泊施設DB → 💾 バックアップ・復元`
6. 画面を目視確認
7. `cleanupHotelDbV2BackupRestoreUiTest()`

UIテストでは専用のテストDriveフォルダを作り、その中に実バックアップと実復元候補を作成します。元スプレッドシートの値・数式・運用シート・APIキーが変わらないことを確認します。

cleanupではUIテストが作ったバックアップ・復元候補・テストフォルダだけをゴミ箱へ移します。既存バックアップや元スプレッドシートは削除しません。

## Out of Scope

- 現在の本番ファイルへの自動上書き復元
- バックアップからの自動差分マージ
- バックアップの自動削除・世代ローテーション
- 外部クラウドへの保存
- Google Places API接続確認

## マージ前条件

- PR #24自己診断 68/68
- PR #23ヘルスチェック回帰テスト成功
- コピー版UIテスト成功
- 実バックアップ作成・検証成功
- 実復元候補作成・検証成功
- 現在の本番ファイル上書きなし
- 元スプレッドシート内容不変
- 運用シート内容・数式不変
- Script Properties APIキー不変
- APIキー本体非露出
- Google Places API呼出なし
- バックアップ・復元画面の目視確認
- cleanup成功

明示的な「マージして！」があるまでマージしません。
