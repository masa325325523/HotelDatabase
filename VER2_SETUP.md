# HotelDatabase Ver2.0

Google Places API (New) を利用して、自治体の宿泊施設一覧を安全に照合する本番運用版です。

## 安全設計

- 元シートの郵便番号・市区町村・住所・施設名は、照合時には変更しません。
- Googleの取得情報は元シート右側へ保存します。
- 差分は `修正候補`、閉業・一時休業・低一致・未検出・APIエラーは `要確認` へ出力します。
- `修正候補` の状態を手動で `承認` にした行だけ、専用メニューから元シートへ反映できます。
- 重複は `重複候補` へ出力し、自動削除しません。
- 50件単位で処理し、次回開始行をドキュメントプロパティへ保存します。

## 必要なApps Scriptファイル

- `Code.gs`
- `ProductionV2.gs`

`Places.gs` はAPI New移行版として残せますが、Ver2.0の本番処理は `ProductionV2.gs` 内の名前空間を利用するため、他の `.gs` ファイルに同名関数があっても影響を受けにくい構成です。

## APIキー

Apps Scriptの「プロジェクトの設定」→「スクリプト プロパティ」に設定します。

- プロパティ: `GOOGLE_PLACES_API_KEY`
- 値: Google Places API (New) を利用できるAPIキー

APIキーをコードやスプレッドシートへ直接記載しないでください。

## 元シートの見出し

1行目に、少なくとも次の列が必要です。

- `施設名`
- `市区町村名` または `市区町村`
- `住所（番地まで）`、`住所`、`所在地`、または `以下住所`

郵便番号は `郵便番号` または `〒` を認識します。

## 初回テスト

1. 本番シートのコピーを作る。
2. コピーしたシートを開く。
3. スプレッドシートを再読み込みして `宿泊施設DB` メニューを表示する。
4. `① Ver2.0 設定・見出し診断` を実行する。
5. `② Ver2.0 API接続テスト` を実行する。
6. `③ Ver2.0 先頭3件テスト` を実行する。
7. 元シート右側と、次の出力シートを確認する。
   - `修正候補`
   - `要確認`
   - `修正履歴`
   - `実行サマリー`

## 修正候補の承認

1. `修正候補` シートで内容を確認する。
2. 反映してよい行の `状態` を `未確認` から `承認` へ変更する。
3. `宿泊施設DB` → `⑧ 承認済み修正候補を反映` を実行する。
4. 反映後は `状態` が `反映済み` になり、`修正履歴` に記録される。

候補作成後に元データが変更されている場合は、自動反映せず `要再確認` になります。

## 本番バッチ

`④ Ver2.0 本番バッチ実行（50件）` を実行します。

- 1回につき最大50件です。
- ダイアログに次回開始行が表示されます。
- 同じメニューを再実行すると続きから処理します。
- 最初からやり直す場合は `⑥ 続きの開始行をリセット` を実行します。

## 重複候補

`⑦ 重複候補を更新` は、次を候補として出力します。

- 同一Place ID
- 施設名＋住所の完全一致
- 同一住所かつ施設名の類似度が高いもの

同一建物内の別客室・別施設を誤削除しないため、自動削除はしません。
