# HotelDatabase Ver2.0

Google Places API (New) を利用して、自治体の宿泊施設一覧を安全に照合する本番運用版です。

## 安全設計

- 元シートの郵便番号・市区町村・住所・施設名は、照合時には変更しません。
- Googleの取得情報は元シート右側へ保存します。
- 差分は `修正候補`、閉業・一時休業・低一致・未検出・APIエラーは `要確認` へ出力します。
- `修正候補` の状態を手動で `承認` にした行だけ、専用メニューから元シートへ反映できます。
- 重複は `重複候補` へ出力し、自動削除しません。
- 50件単位で処理し、次回開始行をドキュメントプロパティへ保存します。

## 必要なApps Scriptファイル

- `Code.gs`
- `V2ConfigApi.gs`
- `V2Sheets.gs`
- `V2Workflow.gs`
- `V2Operations.gs`
- `ProductionV2.gs`
- `V2AddressTests.gs`（住所正規化テスト）

`Places.gs` はAPI New移行版として残せますが、Ver2.0の本番処理は `ProductionV2.gs` 内の名前空間を利用するため、他の `.gs` ファイルに同名関数があっても影響を受けにくい構成です。

## APIキー

Apps Scriptの「プロジェクトの設定」→「スクリプト プロパティ」に設定します。

- プロパティ: `GOOGLE_PLACES_API_KEY`
- 値: Google Places API (New) を利用できるAPIキー

APIキーをコードやスプレッドシートへ直接記載しないでください。

## 元シートの見出し

1行目に、次のうち該当する見出しを置きます。

- 郵便番号（または `〒`）
- 市区町村名（または `市区町村`）
- 住所（番地まで）（または `住所`、`所在地`、`以下住所`）
- 施設名
- 宿泊分類
- 備考

Google情報の列がない場合は、右端へ自動追加されます。

## 住所比較

住所比較では、次の表記差を同一として扱います。

- 全角数字と半角数字
- 半角空白と全角空白
- 空白の有無
- ハイフン、長音、マイナス記号などの違い
- `丁目`、`番地`、`番`、`号` とハイフン表記の違い

元住所と同じ番地の後ろへ、Google側の施設名・施設種別・同じ番地が重複して付いた場合は、住所修正候補にしません。

一方で、建物名、部屋番号、号室、階数、実際の番地が異なる場合は住所差分として残します。

## 初回テスト

1. 7つの `.gs` ファイルをApps Scriptへコピーして保存します。
2. スプレッドシートを再読み込みします。
3. コピーしたテスト用シートを開きます。
4. `① Ver2.0 設定・見出し診断` を実行します。
5. `② Ver2.0 API接続テスト` を実行します。
6. `runHotelDbV2AddressNormalizationTests` をApps Scriptの関数一覧から実行します。
7. `③ Ver2.0 先頭3件テスト` を実行します。
8. `修正候補`、`要確認`、`修正履歴`、`実行サマリー` を確認します。

住所正規化テストはAPIを使用しません。正常なら、次の画面が表示されます。

```text
住所正規化テスト成功

成功件数: 6件
失敗件数: 0件
```

初回テストでは、`⑧ 承認済み修正候補を反映` を実行しないでください。

## 通常運用

- `④ Ver2.0 本番照合（50件）`: 新規照合と続きからの再開
- `⑤ 既存Place IDを再確認（50件）`: 保存済みPlace IDの営業状態・住所などを更新
- `⑥ Ver2.0 続き位置をリセット`: アクティブシートの次回開始位置を2行目へ戻す
- `⑦ Ver2.0 重複候補を更新`: Place ID、施設名、住所から重複候補を作成
- `⑧ 承認済み修正候補を反映`: `修正候補` で状態を `承認` にした行だけ反映

## 修正候補の確認

`修正候補` シートで、元情報とGoogle候補を確認します。

- 採用する行: 状態を `承認`
- 採用しない行: 状態を `却下`
- 判断保留: `未確認` のまま

反映時に元シートの値が候補作成時から変わっていた場合は、上書きせず `要再確認` になります。

## 注意

- Google情報だけで営業実態を完全に保証することはできません。公式サイト、予約サイト、自治体資料などとの最終確認が必要です。
- Googleの施設名や住所が誤っている場合があるため、修正候補は必ず人が確認してください。
- `修正候補`、`要確認`、`重複候補` の行は自動削除しません。
- 同一建物の別室や別施設は、独立営業かどうかを人が判断してください。
