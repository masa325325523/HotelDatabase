# PR #19 承認済み恒久閉業施設の安全なアーカイブ除外

## 目的

⑫で `削除推奨判定=削除候補有力` になり、人が `状態=承認` にした恒久閉業施設だけを、実行直前に元DBとGoogle Placesで再検証し、`閉業除外履歴` に完全保存してから元DBの内容を除外します。

## 最重要の安全原則

- `deleteRow()` / `deleteRows()` は使用しません。
- 元DBの行番号を残したまま、除外対象行の **内容だけ** `clearContent()` します。
- 下の施設の行番号は変わりません。
- Google候補なし、一時休業、開業予定は⑰では除外しません。
- `状態=承認` だけでは除外しません。`理由=閉業`、`削除推奨判定=削除候補有力`、Place ID、一致スコア75以上、候補作成時営業状態=閉業がすべて必要です。
- ⑫で候補作成時の元DB行全体（見出し・表示値・数式）をSHA-256化し、⑰直前に一致を再確認します。
- 候補作成後に元行が1セルでも変われば `要再確認` となり、元DBを変更しません。
- Place Details を実行直前に再取得し、Place ID、恒久閉業、施設名、住所、郵便番号を再確認します。
- 一致スコアも保存値を信用せず再計算し、75点未満なら除外しません。
- アーカイブを保存し、読み戻し検証に成功してから初めて元DBをクリアします。
- クリア後に失敗した場合は、メモリ上の元値・元数式を復元し、元行ハッシュが一致するまで確認します。
- 成功した除外は `修正履歴` にも `承認閉業除外` として記録します。
- 同じ元シートID＋元行を参照する未処理の修正候補・重複候補・新規施設分類候補は `要再確認` にします。

## ⑫で追加する監査列

1. 除外候補作成日時
2. 除外元行ハッシュ
3. 除外処理日時
4. 除外履歴キー
5. 最終Google確認日時
6. 最終Google施設名
7. 最終Google住所
8. 最終Google営業状態
9. 最終一致スコア
10. 除外結果

⑫は削除候補有力になった行だけ元DBの行ハッシュを保存します。元DB・Place ID・状態は変更しません。

## ⑰の処理順

1. 人の承認と削除候補有力条件を検証
2. 元シートID・元シート名・元行を検証
3. 元行ハッシュを再計算
4. 郵便番号・市区町村・住所・施設名・宿泊分類・Place IDを再照合
5. Place Detailsを再取得
6. `CLOSED_PERMANENTLY` を確認
7. Google施設名・住所・郵便番号を再照合
8. 一致スコアを再計算して75点以上を確認
9. `閉業除外履歴` へ32列の完全アーカイブを書き込み
10. 除外キー・ハッシュ・Place ID・施設名を読み戻し検証
11. 元DB行を `clearContent()`
12. 値・数式が空になったことを確認
13. 履歴を `除外済み` に確定し、要確認側も `除外済み` に更新

## 閉業除外履歴

32列固定です。元の郵便番号、市区町村、住所、施設名、宿泊分類、備考、Place IDに加え、候補時Google情報、最終Google情報、元行ハッシュ、元見出しJSON、元行値JSON、元行数式JSONを保存します。

除外キーは以下です。

`元シートID | 元行 | Place ID | 元行ハッシュ`

同じ除外キーが既に `除外済み` の場合は二重除外しません。

## 状態

- `未確認`: 人が未確認
- `承認`: 人が除外を承認
- `要再確認`: 元DBまたはGoogle情報に変更・不一致
- `除外済み`: 正常完了
- `除外エラー`: API・アーカイブ・書込み等の処理エラー

## 自己診断

`runHotelDbV2ApprovedClosedFacilityRemovalTests()`

40ケース以上を対象に、承認条件、元DBスナップショット、Google再確認、75点閾値、除外キー、32列履歴、物理行削除禁止などを検証します。

## コピー版UIテスト

対象は `宿泊施設DB_PR13_⑧反映テスト` のみです。

1. `setupHotelDbV2ApprovedClosedFacilityRemovalUiTest()`
2. ⑫を実行
3. 指定テスト行を承認し、元データ変更ケースを変更
4. `testHotelDbV2ApprovedClosedFacilityRemovalUiTest()`
5. 結果を確認
6. `cleanupHotelDbV2ApprovedClosedFacilityRemovalUiTest()`

UIテストは正常除外、未承認、一時休業、元データ変更、Google営業再開、APIエラー、下行番号保持を確認します。通常の要確認・閉業除外履歴・修正履歴は退避し、cleanupで復元します。

## Out of scope

- 重複候補の実削除
- Google候補なしの自動除外
- 一時休業・開業予定の除外
- 自動承認
- 物理行削除
- 宿泊分類・施設名・住所・Place IDの修正
- 法的宿泊分類の自動判定
