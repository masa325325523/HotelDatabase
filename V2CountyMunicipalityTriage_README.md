# Ver2.0 郡・町村表記差の再判定

`V2CountyMunicipalityTriage.gs` は、自治体データとGoogleの間で「郡」と「町村」を置く列が違うだけのケースを、安全に再判定します。

## 対象例

元データ:

- 市区町村: `鳥取県岩美郡`
- 住所: `岩美町岩井536`

Google:

- 市区町村: `鳥取県岩美町`
- 住所: `岩美郡岩美町岩井536`

この場合、元データの `市区町村 + 住所` と、Google側の `都道府県 + 住所` を結合すると同じ所在地になります。

## 安全条件

次をすべて満たす場合だけ、`推奨判定` を `却下候補` に変更します。

- 現在の推奨判定が `要人確認` または空欄
- 差分が `市区町村・住所` の2項目だけ
- Place IDあり
- Google営業状態が `営業中`
- 一致スコア90点以上
- 郵便番号が正規化後に一致
- 施設名が一致
- 元市区町村が `都道府県 + 郡` 形式
- 元住所の先頭町村とGoogle市区町村が一致
- 元データを結合した住所とGoogle側を結合した住所が一致

`大字` の有無だけは同一住所として扱います。

## 変更しないもの

- B列 `状態`
- 元シートの郵便番号
- 元シートの市区町村
- 元シートの住所
- 元シートの施設名
- 既に `承認候補` / `却下候補` / `対象外` になっている行

この機能は、PR #4の `⑨ 修正候補を自動仕分け` の結果に対する追加の安全な再判定です。

## テスト

Apps Scriptで `runHotelDbV2CountyMunicipalityTriageTests` を実行します。

期待結果:

- 成功件数: 10件
- 失敗件数: 0件
- 状態列の自動変更: なし

テストには次を含みます。

- 岩美郡 / 岩美町
- 八頭郡 / 八頭町
- 東伯郡 / 三朝町
- 日野郡 / 江府町（`大字` の有無）
- 番地違いを拒否
- 町村違いを拒否
- 郵便番号違いを拒否
- 施設名違いを拒否
- スコア90未満を拒否
- 施設名差分を含む複数差分を拒否

## 鳥取テスト手順

1. Apps Scriptに `V2CountyMunicipalityTriage.gs` を追加する。
2. `runHotelDbV2CountyMunicipalityTriageTests` を実行し、10/10成功を確認する。
3. `runHotelDbV2CountyMunicipalityTriage` を実行する。
4. 完了画面の `却下候補へ変更` 件数を確認する。
5. `修正候補` シートで、岩美郡・八頭郡・東伯郡・日野郡などの候補が想定どおり `却下候補` になったか確認する。
6. B列 `状態` が変更されていないことを確認する。
7. 検証中は `⑧ 承認済み修正候補を反映` を実行しない。

## 今後

鳥取で安全性と削減効果を確認した後、必要であればこのロジックを住所比較本体へ統合し、新規バッチ生成時点から不要な修正候補を減らします。
